# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## ⚠️ Living documentation contract (READ FIRST, applies every session)

myHealth is an **evolving project**. These three documents are the source of truth and **must be kept current as the project grows**:

- `README.md` — user-facing overview, status, feature summary, layout.
- `CLAUDE.md` — this file: agent guidance, architecture, constraints, conventions.
- `docs/PRODUCT_FEATURES.md` and `docs/PLAN.md` — the product + plan specs.
- `docs/SHARED_CORE.md` — how this app relates to the shared cross-app core package.

**The rule:** whenever you complete a **major feature add or an architectural change**, you MUST, in the *same* piece of work, update the affected docs above before considering the task done. "Major" = a new top-level feature/page/module, a new migration set, a new subsystem (e.g. the import sidecar), a new gate/tier, a new master/OTA bundle type, or any change that makes the existing docs inaccurate.

Concretely, on each major change:
1. Update `CLAUDE.md` — add/adjust the relevant architecture section, the migration count, the data-model sketch, and the "current state" line below.
2. Update `README.md` — feature list, status, and roadmap if the phase advanced.
3. Update `docs/PLAN.md` (roadmap/data-model) and `docs/PRODUCT_FEATURES.md` (feature catalog) if scope shifted.
4. Mention the doc updates in your summary so they're visible in review.

Do **not** treat docs as optional cleanup. A feature is not "done" until the docs describe it. If a change is small/mechanical (a bugfix, a rename, a style tweak), no doc update is needed — use judgement.

**Current state:** _Phase 1 complete._ Tauri+React+TS app wired to `sharedcorelib` (vault, tiers, gating, reminders sweep, ice, report, env, ui, db/schema, common masters). Migrations 0001–0009. Working: **Today** (water + daily tasks), **Profiles** (self+family), **Vitals**, **Reminders** inbox, Tracker-tier **Goals** (deterministic ETA — `domain/goals.ts`) / **Schedule** / **Trends** (charted vitals with reference-range bands + a deterministic trend/sentiment summary — `domain/trends.ts` + `pages/Trends.tsx`, recharts), Caretaker-tier **Medications** / **encrypted Document vault** (Stronghold unlock + AES-GCM blobs via `sharedcorelib/vault`) / **Medical ICE card** + **doctor-visit summary** (`lib/visitReport.ts` → HTML→PDF via `sharedcorelib/report`), **Journey** (tier ladder), the progressive-disclosure shell (Open/Nudge/Hidden + `FeatureGuard`), and the **habit reminder sweep** (water/task/medication → one OS notification per sweep, on idle). The shell now also carries the suite-standard **"Report an issue"** action (prefilled GitHub issue via `lib/reportIssue.ts` + `lib/openExternal.ts`) and the **"Supported by Tokans.org"** attribution line (`sharedcorelib/ui` → `SupportedByTokans`). The **Medical ICE card is now a shared common table** in the suite DB: `common#IceCard` (`sharedcorelib/ice` → `ICE_CARD_SCHEMA` + `createIceStore`), keyed per person, written/read by both myHealth and myFinance through the shared `suite.db` (Rust `shared_core_db_path` + `db/sharedDb.ts`); whichever app launches first creates it. **Progressive disclosure now reveals exactly one tier ahead** (`gateVisibility` in `src/lib/featureGate.ts`): a tier-gated feature is shown-locked (**Nudge**) only to users *exactly one tier below* it and **Hidden** to anyone further down — so a Starter sees the Tracker features (Goals, Schedule, Trends) locked-with-CTA but **nothing** of Caretaker (Medications, Documents, ICE) or Champion (Import, Directory, sync, items); a Tracker sees Caretaker locked but not Champion; and so on. Each tier gate declares its `tier` (`tracker`/`caretaker`/`champion`) and `gateVisibility(gateKey, flags)` derives Open/Nudge/Hidden from the gap to the user's highest cleared tier; non-tier *prerequisite* gates (e.g. `family`, gated on having your own profile) keep a static `lockBehavior`. AppShell's `navState` delegates to `gateVisibility`. (The tier mapping was reassigned: Medications + Documents moved Tracker→Caretaker, and Import + Directory moved Caretaker→Champion.) The **Newcomer (Starter) surface stays water-first:** only **Today** (water + tasks), **Vitals**, **Reminders**, **Profiles**, and **Journey** are Open. **Active profile is now a global store** (`src/stores/profile.store.ts`, selected id persisted to localStorage) backing `useActiveProfile`, so switching the active person re-scopes every page; in `nav.ts`, `primary` marks the mobile bottom-bar **home** tab (Today) and `central` marks the destinations grouped under the heart FAB (Reminders/Goals/Schedule). **Vitals is no longer a center tab** — it's reached per-profile: each card in `Profiles.tsx` (and a freshly created profile) carries a **"Vitals"** action that sets the active profile and jumps to `/metrics`; Vitals also stays in the More drawer + desktop sidebar. The **app shell is now the shared `SuiteShell`** (`sharedcorelib/ui`): `AppShell.tsx` is a thin adapter that feeds it nav data (with precomputed open/nudge `state`), `centralActions`, `actions`, a tier badge, and the `profile` slot. Layout = desktop sidebar + mobile top bar (brand left, profile right) + a fixed three-button bottom bar **[Today (home) · ♥ Health (center) · More]** (the center button is adaptive — a plain button at 1 action, a heart **FAB + bottom sheet** at 2+; the sheet groups the `central`-flagged destinations **Reminders · Goals · Schedule**, gated ones surfacing only once unlocked, otherwise staying in More as a nudge); the **More** drawer (built into `SuiteShell`) holds the remaining destinations + "Report an issue" + the Tokans attribution. The top-right `profile` slot is `ProfileMenu.tsx` (avatar button + the family `ProfileDrawer.tsx`); `components/ui/sheet.tsx` is now a re-export shim of the shared `Sheet`, and the old `MoreDrawer.tsx` was deleted (superseded by `SuiteShell`'s built-in drawer). Theming is the shared **§4.2** model: `tailwind.config.cjs` uses `sharedcorelib/tailwind-preset` + adds `../sharedCoreLib/src/ui/**` to its content globs, and `src/index.css` imports `sharedcorelib/ui/theme.css` then overrides only myHealth's ~5 teal brand tokens. The first slice of **Phase 2** has landed: the **document-import field-extraction layer** (`src/import/`) — a pure, deterministic engine (no DB/network/models/LLM) that turns *recognized* prescription/lab text into structured, confidence-tiered, human-confirmable fields. It fuzzy-matches against a small India-context drug **formulary** (`formulary.ts`, brand→generic) and a **LOINC-style lab vocabulary** (`labVocab.ts`), tolerates OCR noise (`normalize.ts`: O/0, l/I/1, S/5 fixups; India-style positional `1-0-1` dosing; en-dash ranges; digits embedded in names like HbA1c), tiers each field `auto`/`disambiguate`/`manual` (`confidence.ts`), and keeps drug/dosage **confirm-required** (`verified:false`). Entry points: `extractPrescription` / `extractLab` / `parseDocument` (`index.ts`). It is decoupled from recognition: the upstream capture/normalization/**OCR sidecar** (parser architecture doc §6.1–6.5; sidecar-vs-native still pending) is the remaining Phase-2 work, so extraction is tested today against deliberately difficult OCR text fixtures (`src/import/__fixtures__/documents.ts`). The whole app is now covered by a **Vitest** suite (jsdom + `@testing-library/react`): **328 tests** spanning domain logic, all `db/` wrappers (mocked `./client`), every store, the React components + `useActiveProfile`, and the import extractors — config in `vitest.config.ts`, setup in `src/test/setup.ts`, tests colocated as `*.test.ts(x)`. `npm run build` + `npm run test` (328 tests) green. Migrations remain **0001–0009** (the extraction layer added no schema). Next: the import **OCR/capture sidecar** + confidence-tiered review UI to feed `src/import` (`docs/PLAN.md` §4), plus OTA masters (Phase 4). Gated placeholders remain for import/directory. Update this line as phases land (see `docs/PLAN.md` §6).

**Stage C migration (branch `build/stageC-migration`, sharedCoreLib v0.2.0):** myHealth now sits on the suite's **shared-entity spine**. Profiles (self + family) and **pets (modeled as managed-dependent `person` rows with `relationship_to_self = 'pet'`)** reference core **`person`** (`sharedcorelib/entities`); the medical data myHealth OWNS lives in a per-app **`myhealth#HealthFacet`** table keyed by `person_key` (sex/blood_group/height/conditions/medications/allergies/advance_directive/organ_donor/is_pet) — field-level ownership, with contact staying on `person`/the common ICE card (finance/ICE precedent). Wiring: `src/db/healthFacet.ts` (facet schema + store), `src/db/entities.ts` (`createHealthPeople`: person spine + facet + legacy-profile migration), registered through `src/db/schemas.ts` (`MYHEALTH_SCHEMAS` now = ICE card + the common `ENTITY_SCHEMAS` + the health facet; the only myHealth-owned table is the facet, the rest are common/non-duplicated) and `src/db/sharedDb.ts` (`healthPeople()`/`healthTimeline()`/`healthBreakGlassContributor()`). myHealth is the suite's **second consumer of core break-glass** (`src/breakglass/contributor.ts`): a `BreakGlassContributor` exposing emergency medical (nominee), advance_directive (executor) and full notes (full) — sealed by core under an out-of-band passphrase; **no health data egresses**. Medical documents + visits/appointments project onto the shared `document`/`event` spine (`src/db/sharedTimeline.ts`) — only the OPAQUE vault `blob_ref` is stored; **document bytes stay AES-GCM under the per-device key**. Layered **recovery** adopted (`src/vault/recovery.ts`: free on-device floor + optional registered-tier ciphertext escrow). Person-linked **reveal ladder + myLifeAssistant nudge** (`src/lib/reveal.ts`). **OpenMed (Phase 4) is a capability-detection STUB only** (`src/import/openmed.ts`) — the Python sidecar isn't built; the free deterministic extraction path is UNCHANGED and the stub always reports the sidecar absent (no network, double-gated on sidecar + entitlement). (Superseded by the DB-consolidation note below: the app-local `myhealth.db` was retired into the shared `suite.db`.) +42 Stage-C tests (`*.test.ts` colocated).

**Excel backup/restore (branch `build/excel-backup`, sharedcorelib v0.3.0):** a new **Settings** page (`/settings`, listed in the More drawer) hosts the suite-standard whole-store backup — `sharedcorelib/backup` subsystem #22 via the shared `BackupPanel` (`sharedcorelib/ui`). It exports the FULL `suite.db` (a sheet per table) and re-imports it on another machine; Secret-tier/password-named fields export as one-way sha256 fingerprints and are skipped on import; vault blobs are never exported. Wiring: `src/lib/excelBackup.ts` (+ `openSharedDbAdapter` in `db/sharedDb.ts`), `src/pages/Settings.tsx`.

**DB consolidation (branch `build/consolidation`, sharedcorelib v0.4.0):** the per-app `myhealth.db` is **GONE** — there is now ONE database, the shared `suite.db`, with every former myHealth table as an app-owned **namespaced `myhealth_*`** table (decision 1). The Tauri-plugin migration array (0001–0009) in `lib.rs` is retired; tables are defined as **semantic `SchemaDescriptor`s** (`src/db/appTables.ts`, registered via `registerSchemas`) plus an **aux-SQL** step (`src/db/auxMigrations.ts`, via `registerAuxMigrations`) for what descriptors can't express — composite/unique indexes, FK ON DELETE CASCADE/SET NULL re-expressed as triggers, and enum CHECK guards. `src/db/tables.ts` is the single source of namespaced names; `src/db/client.ts` opens `suite.db`. Confidentiality tags: medical tables `Restricted`, wellness/engagement `Confidential`, settings/telemetry `Internal`, and `documents.extracted_text` → **`extracted_text_enc`** which is **vault-sealed** (`Secret`). **`extracted_text` sealing (#26):** sealed at rest via `src/db/sealedText.ts`, which reuses the AUDITED per-device DEK primitive `vault.sealBytes`/`openBytes` (AES-256-GCM — the SAME key that seals doc blobs); readers decrypt transparently, AAD binds the cell to the blob uuid; backup exports it as a sha256 fingerprint only. **Spine collapse (invariant 6):** legacy `profiles` lifts onto `common_person` + `myhealth_health_facet`, keyed by a thin `myhealth_profiles` link (the historical integer id ↔ `person_key`); `profiles.ts` reads JOIN link+person+facet into the flat `Profile` shape and writes fan out. A **one-time migrator** (`src/db/consolidate.ts`, wired into App boot) copies the legacy file table-by-table (idempotent/resumable; keys preserved; `extracted_text` sealed in transit), verifies row counts + a checksum sample into `myhealth_migration_ledger`, then **deletes** `myhealth.db` (decisions 6/24, executed-pending-review). Backup is now a single `suiteSourceFull` source. No health data egresses.

**Shared scanned-document reading engine (branch `build/scandoc-lib`):** the domain-agnostic reading primitives that lived in `src/import/` — `normalize.ts` (OCR-noise cleanup), `fuzzy.ts` (Levenshtein/`rankMatches`), `confidence.ts` (tier + confirm-required) — were extracted into a new standalone package **`@scandoc/core`** (`sharedCoreLib/scandoc-lib`, added the same way as `sharedcorelib`/`@form-engine/react`: `file:../sharedCoreLib/scandoc-lib`), so **myDocs** and any future document-reading app reuse one engine instead of forking it. The package also adds a typed **`Recognizer` seam** — the plug point the future OCR/capture sidecar will implement (no OCR engine ships yet; `nativeTextRecognizer` is the offline identity recognizer) — and an optional `Field<T>`/`fieldFrom` extraction-framework contract. myHealth keeps ONLY its health-specific extractors/vocabulary in `src/import/` (`extractPrescription`/`extractLab`/`parseDocument`/`formulary`/`labVocab`/`openmed`), importing the engine from `@scandoc/core`; `import/index.ts` re-exports the moved symbols so the `import/` surface is unchanged for pages/tests. `requiresConfirmation` is now `(source, safetyCritical)` with myHealth passing `true` for drug/dosage. No behavior change, no new runtime deps, no egress; the 3 moved unit-test files now live in the lib (myHealth `npm test` = 301, was 325). `prebuild` now also builds `scandoc-lib`.

**Documents = Imports, + insurance-card scan (branch `build/excel-backup`):** the separate Champion-tier **Import** placeholder was **removed and merged into the Caretaker-tier Documents vault** — importing *is* adding a document, so there is one feature now. The `import` gate/`GateKey`, its nav item, and its route are gone; `/import` redirects to `/documents`. The Documents page (`src/pages/Documents.tsx`) gained a **scan & extract** flow: on add, text is recognized through the `@scandoc/core` **`Recognizer` seam** (`src/import/capture.ts` → `recognizeDocument`; text files pass through as `native-text`, images/PDFs come back empty + `ocr` for the user to paste until an OCR sidecar lands), then `parseDocument(kind, …)` structures it for prescription/lab/**insurance**. New extraction: **`src/import/extractInsurance.ts`** (policy fields + covered-members, India-context **`insurerVocab.ts`** fuzzy insurer match, OCR-tolerant `normalizeDate`, card-term→relationship mapping) and **`src/import/memberReconcile.ts`** (pure name-based reconcile of card members against existing profiles; `"self"` maps to the self profile). A **"Scan insurance card"** action runs the flow preset to `insurance`: detected members are shown in a **propose-then-confirm** review list (editable name/relationship/DOB, pre-checked only when *missing* from the family) and the confirmed ones are created via `createProfile` (legacy `profiles` table — the path Profiles/active-profile read), so **the scan adds missing family members**. Members are always `confirmRequired` (creating a person is user-confirmed); no health data egresses; vault blobs are still never exported and only the extraction's text summary lands in `documents.extracted_text` (column already existed → no migration). +18 tests (`extractInsurance.test.ts`, `memberReconcile.test.ts`). `parseDocument` kind union now includes `"insurance"`.

**Mobile camera scan (opt-in) — branch `build/excel-backup`:** the Documents add/scan panel now offers a **"Scan with camera"** button on phones, beside "Choose file". It uses the webview's **native file input** (`<input type="file" accept="image/*" capture="environment">`) — no Rust camera plugin — and the captured photo flows into the **same encrypt-on-save path** as a picked file (`acceptDocument`, shared by `pickFile`/`onCameraCapture` in `Documents.tsx`); bytes never leave the device, then OCR (below) reads the text. The button is **double-gated**: shown only when `isMobile()` AND the user enabled it. The toggle is a new **opt-in setting** — `cameraScan` in `settings.store.ts` (persisted to the schemaless `settings` key/value table as `camera_scan`, **no migration**; `setCameraScan` upserts) surfaced as the **"Document scanning"** card on `/settings` (`ScanningCard`, off by default). iOS needs a camera-usage string, so `scripts/ensure-ios-plist.mjs` now also injects `NSCameraUsageDescription` (idempotent). +4 tests (`settings.store.test.ts` hydrate/persist). Migrations stay **0001–0009**.

**Basic on-device OCR — free, in `@scandoc/core/ocr` (branch `build/excel-backup`):** the `Recognizer` seam now has a real, FREE implementation — **Tesseract** (`tesseract.js`, WASM in the webview, NO native sidecar). It lives in the shared **`@scandoc/core/ocr`** subpath (reusable by myDocs/etc., kept separate from the pure text engine so non-OCR consumers don't pull the heavy deps): `createTesseractRecognizer(config, deps)` (drops into `recognizeDocument`), `provisionLang`/`isLangProvisioned`/`sha256Hex` (download-once + verify), `rasterizePdf` (pdf.js page→canvas), and the `OcrAssetHost`/`TesseractRecognizerConfig`/`OcrProgress`/`OcrCancelled`/`OcrIntegrityError` contracts. **DI discipline:** the lib carries the orchestration but NONE of the host — the app injects all I/O (`OcrAssetHost`) AND the heavy peerDeps themselves (`loadCreateWorker: () => import("tesseract.js")`, `rasterize` over an app-imported `pdfjs-dist`), because a bare `import("tesseract.js")` from the symlinked lib dist wouldn't resolve to the app's copy and only the app's bundler can code-split them. **Download-once:** the tesseract worker + core wasm + pdf.js worker are app-bundled into `public/ocr/` (generated by `scripts/build-ocr-assets.mjs copy`, run in `prebuild`, gitignored); only the ~10 MB **`eng.traineddata.gz`** is downloaded once at runtime (Tauri HTTP), **SHA-256-verified before write** (engine throws `OcrIntegrityError` on mismatch — verify-before-persist), cached under `BaseDirectory.AppLocalData/ocr/`, and reused offline forever (tesseract `langPath` = the asset-protocol dir URL via `convertFileSrc`). myHealth glue: `src/import/ocr/` — `config.ts` (env + bundled paths; `ocrConfigured()`), `tauriHost.ts` (the `OcrAssetHost`), `index.ts` (`makeOcrRecognizer`/`ocrLangProvisioned`, injecting the peerDeps). `capture.ts` `recognizeDocument(bytes, mime, name, { onProgress, signal })` now derives `needsOcr = text.trim() === ""` and routes image/scanned-PDF bytes to the OCR recognizer **only when `isTauri() && ocrConfigured()`** (else the paste fallback); OCR output keeps `source: "ocr"` so drug/dosage/lab stay **confirm-required** (OCR is never authority). UI: `Documents.tsx` runs OCR on pick/capture for extractable types with **progress + Cancel** (`AbortController`) and a **one-time consent** ("Downloads ~10 MB once, then offline") gated by a new `ocrConsent` flag in `settings.store.ts` (schemaless `settings`, **no migration**). The paid **OpenMed AI extraction stays a separate, subscription path** (`src/import/openmed.ts` stub) — this is the free deterministic OCR floor. Config: `VITE_OCR_ASSET_BASE_URL` + `VITE_OCR_TRAINEDDATA_SHA256` (publish the `.gz` + bake its hash via `npm run ocr:traineddata`); double-gated with Tauri, so dev/browser/tests are a clean no-op. Tauri: `tauri.conf.json` CSP gains `script-src 'wasm-unsafe-eval'`, `worker-src 'self' blob:`, `img-src … blob:`, `connect-src … asset: <github-release hosts>` + an `assetProtocol` scope `$APPLOCALDATA/ocr/**`; the GitHub-release download hosts were already in the `http` capability. `vite.config.ts` routes `tesseract.js`/`pdfjs-dist` to a lazy **`vendor-ocr`** chunk (~415 KB, loaded only on first OCR). New deps: `tesseract.js@5.1.1` + `pdfjs-dist@4.10.38` (myHealth) / **peerDependencies** (scandoc-lib). +17 OCR tests (`scandoc-lib` `test/ocr-assets.test.ts` + `test/ocr-tesseract.test.ts` = 13; myHealth `src/import/capture.test.ts` = 4) plus the `ocrConsent` settings cases. Migrations stay **0001–0009**.

**Content framework — folder-driven tabs + daily-synced downloadable bundles (Tracker tier):** the one-off Yoga page was generalized into a reusable **content library**. Each top-level **`content/<type>/index.ts`** default-exports a `ContentType` (label, Lucide icon, `tier`, `releaseTag`, baked `samples`); the registry (`src/content/registry.ts`) **auto-discovers every folder via `import.meta.glob('/content/*/index.ts')`**, so **dropping a folder adds a tab with no wiring**. Ships two baked types — **`content/yoga/`** (sequences, art in `content/yoga/art.ts`) and **`content/exercises/`** (workouts) — plus `content/<type>/bundles/*.json` (the downloadable packs, NOT imported → not in the build) and `content/catalog.json` (remote type catalog). Model: `src/content/model.ts` (`ContentType`/`ContentEntry`/`ContentStep`/`ContentBundle` + pure helpers `totalDurationSec`/`formatDuration`/`mergeEntries`/`bundleEntries`/`stepImage`); `tsconfig` now includes `content`. One **generic page** `src/pages/Content.tsx` at **`/content/:type`** (e.g. `/content/yoga`; `/yoga` redirects) renders any type: tier-guarded via the new **`tierVisibility(tier, flags)`** (extracted from `gateVisibility` in `featureGate.ts`), lists baked ⊕ downloaded entries (baked wins id collisions), opens each into a numbered, illustrated step view, and has a **bundle manager** with an **available-vs-installed** model (`BundleManager` in `Content.tsx`): "Check now" refreshes the per-type **available catalog** (`availableByType`) and tapping a bundle **installs** it (`installBundle`, available→`bundlesByType`); **Remove** uninstalls but the bundle stays available to re-add; content bundles have **no anti-rollback** (the sync passes `lastRevision 0`) so the catalog always refreshes and a removed bundle reappears — already-installed bundles auto-update to the latest version. Content tabs are generated by `buildNav(contentTypes)` (`src/lib/nav.ts`) and spliced after Trends; they are **mobile-FAB (`central`) destinations**, not "More" entries (AppShell). **Daily background sync** (`src/content/updater.ts` → `runContentSync`, called on idle at startup, self-throttled to once/day): pulls a signed **remote catalog** (registers brand-new types without an app update) then each type's **bundles**, via the shared `sharedcorelib/masters` engine (Ed25519 verify → revision/app-version gate → per-file SHA-256 → AES-256-GCM decrypt → zod-validate `src/content/schema.ts`, image URLs restricted to `data:`/`https:`). State in `src/stores/content.store.ts` (bundles + revisions per type, remote types, catalog revision, last-checked) is **persisted to localStorage** (public, non-sensitive reference data — no health PII, so **no SQLite migration**; migrations stay 0001–0009). Sync is **double-gated**: disabled unless content signing keys are configured (`VITE_CONTENT_PUBKEY`/`VITE_CONTENT_TRANSPORT_KEY`, `VITE_CONTENT_BASE_URL`/`VITE_CONTENT_CATALOG_TAG`) AND running in Tauri — baked samples always work. Publishing: **`scripts/build-content.mjs`** (`keygen`/`build-type <type>`/`build-catalog`/`build-all`, Node built-ins only) packs `content/<type>/bundles/*.json` + `content/catalog.json` into signed release assets per tag `content-<type>-latest` / `content-catalog-latest` (verified round-trip against the real engine). Movement/activity guidance only — disclaimers on the page, no medical interpretation, receive-only (uploads nothing). **The engine is now promoted to shared core (`sharedcorelib/content`)** so sibling apps reuse it: the model + helpers, the OTA payload schemas, the registry merge (`collectBakedTypes`/`mergeTypes`/`findContentType`), the **`createContentStore`** store factory, the **`createContentSync`** OTA factory, AND an **arbitrary-depth content-TREE** reader (`sharedcorelib/content/tree`: `buildContentTree`/`buildContentTreeFromGlob` + `parseProperty`/`parseSimpleYaml` + `nodeAt`/`nodeLabel`/`nodeOrder`/`nodeEntries`/`leaves`/`prop*`, re-exported from `@/content/model`) all live in core. The **tree** generalizes `content/<type>/` to ANY depth (`content/yoga/morning/…` here; `content/[board]/[class]/[subject]/[book]/…` in myEducation): every folder is a node, the FILES in it are properties (filename→name; value parsed by extension — json/yaml/text/file-ref), subfolders are children, leaves carry the main content (an `entries.json`). Core ships fixtures + tests for both a shallow (myHealth) and a deep (myEducation) shape (`sharedcorelib` `src/content/__fixtures__/` + `tree.test.ts`); myHealth has a binding test `src/content/tree.test.ts`. **Yoga and Exercises now carry SUBTYPES** (`ContentType.tree`, built inline with `buildNodeTree` in `content/<type>/index.ts` — Yoga → Morning/Relax/Balance, Exercises → Mobility/Strength; `samples:[]`). The generic page renders a slim **breadcrumb + next-node dropdown** navigator (`src/components/content/ContentTreeNav.tsx`) when a type has a tree: pick a section to descend, leaves list their entries, downloaded bundles surface at the root. Flat types (no `tree`) are wrapped in a single-leaf root so the page code is uniform. **Testing the "Check now" download locally:** sample bundles live in `content/<type>/bundles/*.json` (yoga: restorative/evening-flows/desk-breaks; exercises: core-strength/cardio-starter); **`npm run content:dev`** ([scripts/content-dev.mjs](scripts/content-dev.mjs)) keygens a throwaway dev keypair (cached in `.dev-content/`, gitignored), builds+signs every bundle + the catalog into the `${baseUrl}/${tag}/…` layout the app fetches, serves them at `http://localhost:4321`, and writes `VITE_CONTENT_*` to `.env.local` — then either **`npm run dev`** (browser preview — a DEV-only `window.fetch` path in `src/content/updater.ts` reuses the core verify/decrypt via the new `createContentSync({fetchBytes})`, so Check now works without Tauri) or **`npm run tauri:dev`** (real Tauri-HTTP path), and click **Check now** (downloaded entries surface at the type root). The button enables on `contentSyncAvailable()` = `sync.canRun()` (configured AND in Tauri OR a dev `fetchBytes` is injected); inside Tauri the core ignores `fetchBytes` and uses the Tauri-HTTP updater (the webview's own fetch is CSP-bound). The dev host `http://localhost:4321/*` is allow-listed for http in `src-tauri/capabilities/default.json` for this (localhost = the user's own machine, no remote egress; remove for a hardened release). `npm run content:build` produces the same signed assets under `dist/content-release/<tag>/` to upload to real GitHub release tags (`content-<type>-latest` / `content-catalog-latest`); `scripts/build-content.mjs` now emits tag-named folders. myHealth keeps only the app-specific bindings — `src/content/model.ts`/`schema.ts` re-export core, `src/stores/content.store.ts` = `createContentStore<EarnedTier>({storageKey})`, `src/content/registry.ts` runs the app's own `import.meta.glob` + icon resolver and calls the core merge, `src/content/updater.ts` = `createContentSync({...app env + store + listTypes})`, plus the `src/content/icons.ts` map, the generic page, and the nav/FAB wiring. (Vitest dedupes `react`/`react-dom`/`zustand` so a core-created store binds the app's single React copy — see `vitest.config.ts`.) +~60 tests (`content/*.test.ts(x)`, `pages/Content.test.tsx`, + `sharedcorelib` `src/content/index.test.ts`); `npm test` (384) + `npm run build` green.

**Disclosure promotion + grant tiers + device sync (sharedcorelib v0.5.0):** three shared-core integrations landed together. (1) **Progressive-disclosure logic promoted to core** — the one-tier-ahead Open/Nudge/Hidden rule that lived in `src/lib/featureGate.ts` is now `sharedcorelib/gating` (`GateVisibility`, `TieredGate`, `rankVisibility`/`tierVisibility`/`gateVisibility`, and the app-injected `TierDisclosure` of `rankOf`/`clearedRank`). `featureGate.ts` keeps myHealth's `GATES`/`TIER_RANK`/`earnedRank` and is now a thin wrapper passing a `DISCLOSURE` adapter to the core resolvers — `HealthGate = TieredGate<GatingFlags, GateKey, EarnedTier>`; the public surface (`gateVisibility(key, flags)` / `tierVisibility(tier, flags)`) is unchanged so `AppShell`/`nav` callers are untouched. (2) **Supporter/Verified Pro tiers now activate from a received grant** (`sharedcorelib/grant`): `src/grant/receiver.ts` wires `createGrantReceiver` with baked keys (`VITE_GRANT_PUBKEY`/`VITE_GRANT_TRANSPORT_KEY`) — verify-then-parse a chosen `.grant` file (Ed25519 over ciphertext → AES-GCM decrypt → zod `healthGrantSchema`), map `patron→supporter` / `partner→pro`, persist a local `GrantStatus` (localStorage, never transmitted, additive). `tier.store` reads it for `isSupporter`/`isPro`; `gating.store` gains an `override` so a Supporter/Pro **unlocks every feature** (donation accelerates, never paywalls the safety floor). Settings hosts a **"Support myHealth"** card (`SupporterCard`): always-visible **Donate** + **Become a Verified Pro** CTAs (external tokans.org links via `src/lib/donate.ts` — `openDonatePage`/`openPartnerSignup`, mirroring myFinance) plus the **"Import support file"** button (enabled only when keys are configured AND in Tauri). Receive-only — nothing is uploaded. (3) **Champion-tier device-to-device Sync** (`sharedcorelib/sync`): the previously dangling `sync` gate now has a real page (`/sync`, `src/pages/Sync.tsx`, `FeatureGuard`-guarded, nav after "Find a Pro"). `src/sync/engine.ts` builds the lib's `createMergeEngine` over the **shared `suite.db`** scoped by the registry to myHealth-owned (`HealthFacet`) + `common` spine tables (LWW, `updated_at` + device-id tie-break via `src/sync/device.ts`); `src/sync/transport.ts` is the v1 **encrypted-file channel** — seal the outgoing bundle with a user **pairing code** via `sharedcorelib/crypto` (PBKDF2→AES-256-GCM, AAD-bound), write a `.sync` file the user carries to the other device, and merge it there. NO backend; document bytes stay in the vault (never exported); a Rust LAN sidecar implementing the lib's `SyncTransport` is the future additive swap. **No SQLite migration** (migrations stay **0001–0009**; grant/device/sync state is localStorage or the existing shared DB). +11 tests (`gating/disclosure` in the lib; `grant/receiver`, `sync/device`, `sync/transport` in the app); `npm test` (**384**) + `npm run build` green.

**Per-tab, human-friendly Excel import/export — scoped to the active profile (branch `build/excel-backup`):** distinct from the whole-store backup on Settings (`@/lib/excelBackup` → `sharedcorelib/backup`, a raw one-sheet-per-table disaster-recovery dump), each of five tabs — **Reminders, Goals, Schedule, Medications, Vitals** — has its own **Export / Import** button pair (`src/components/feature/ExcelButtons.tsx`) in the page header that reads/writes a small, hand-editable `.xlsx` **for the currently selected profile only** (`myHealth-<feature>-<profile>.xlsx`, one sheet; the buttons disable until a profile is active). The format is friendly, not table-shaped, and carries **no `profile_id`/Person column** (the whole file belongs to one person): `HH:MM` instead of `start_min` minutes, "Every day"/"Weekdays" instead of `daily`/`weekdays`, "Lower is better"/"Higher is better"/"Maintain" for goal direction, "Yes/No" for the medication active flag, metric **labels** ("Weight") instead of kinds. Re-import is **update-by-ID + add-new, confined to that profile**: an ID matching one of *this profile's* records UPDATES it; any other ID (blank, or one belonging to another person) is INSERTED as new **under the active profile** — so a sheet can never overwrite another person's data (covered by a cross-profile scoping test). **Reminders export includes the auto/derived nudges** (water/task/medication) for that person alongside manual ones with an **`Auto` column** so the sheet mirrors the inbox; `Auto`=Yes rows are skipped on import (the app regenerates them) — exporting manual-only previously produced a confusing header-only file. **Empty export still writes a header-only, fillable template** (via `aoa_to_sheet`) and the UI says "saved a blank template you can fill in" rather than looking broken; `exportFeatureWorkbook(spec, { profileId })` returns `{ bytes, rowCount }`. Engine: `src/lib/featureExcel.ts` — a lazy-imported SheetJS codec (`exportFeatureWorkbook`/`parseFeatureWorkbook`) plus the `FEATURE_EXCEL` registry of five `FeatureExcelSpec`s (each `exportRows(ctx)`/`importRows(rows, ctx)` takes an `ExcelContext = { profileId }`; friendly column map + per-feature upsert with a per-row `ImportSummary` of added/updated/skipped + warnings; non-numeric/empty required cells skipped with a row-numbered warning). Profile-scoped DB helpers back it: `listRemindersForExport(profileId)`/`updateManualReminder`, `listGoals`/`updateGoal`, `listBlocks`/`updateBlock`, `listMedications(profileId,false)`/`createMedicationFull`/`updateMedication`, `listMetricsForProfile(profileId)`/`updateMetric`. **`xlsx` (SheetJS 0.20.3) is now a direct dependency** (was only transitive via `sharedcorelib`). Desktop-only (OS file dialogs + DB; `ExcelButtons` renders nothing in the browser preview); on-device only — nothing is uploaded; vault document bytes are never part of these sheets. **No SQLite migration** (migrations stay **0001–0009**). The **Reminders inbox page is now profile-scoped too** (`listOpenReminders(profileId?)` — the page passes the active profile and reloads on switch; the household-wide OS-notification sweep still calls it with no argument), so it matches the other pages' active-profile re-scoping. The inbox already shows the owner (the derived reminder's `detail` = the person's name, rendered in the sub-line); the **OS nudge now names the person too** — `runHabitReminderSweep` prefixes each open reminder's title with its owner (`"Asha: Drink water …"`) when the household has more than one profile (single-profile = self, left unadorned), so the single summary notification says for whom each line is meant (the shared sweep builds its body from `r.title`; we map a copy, leaving the stored title and `markFired` id untouched). +16 tests (`src/lib/featureExcel.test.ts` per-profile/empty-template/auto-reminder; the `listOpenReminders` scoping case; `src/lib/reminderSweep.test.ts` nudge-naming); `npm test` (**405**) + `npm run build` green.

**App marketplace + Settings relocation (`sharedcorelib/suite`):** wired the suite **app marketplace** (`createAppCatalog`) as a new **Apps** page (`/apps`, `src/pages/Apps.tsx`). It lists every Tokans app — installed or not — from a baked published-apps seed (`src/suite/config.ts` → `SEED_PUBLISHED_APPS`) unioned over an optional cached signed registry (`src/suite/registry.ts`; OTA-delivered later, falls back to the seed so the current app is always present). DI adapters: `src/suite/localState.ts` (per-app-private installed/version/phone-sync in localStorage), `src/suite/version.ts` (Tauri app version), `src/suite/platform.ts` (UA-based download-link pick), and `src/suite/catalog.ts` (`suiteCatalog`). **Entitlements come from the grant state** — `grantStatus()` maps Supporter→`isPatron`, Verified Pro→`isPartner` — so an access-gated paid sibling (`mylifeassistant` = `patron`, `myworkassistant` = `partner`) shows **Enroll** until the matching grant is imported, then **Download**; the same grant that unlocks features also unlocks paid apps here. Each row's `primaryAction` (`current`/`open`/`download`/`enroll`) drives its button; clicks are OS-browser handoffs of static marketing/download URLs (allowlisted github.com/tokans/* + tokans.org/*) — **no account, no egress, no health data**. The marketplace **replaced Settings in the shell action slot** (`AppShell.tsx` `actions`: `LayoutGrid` "Apps" → `/apps`), and **Settings moved into the profile drawer** (`ProfileDrawer.tsx`, beside "Your journey"); the `/settings` route is unchanged. **No SQLite migration.** +4 tests (`src/suite/catalog.test.ts`); `npm test` (**398**) green.

**Demo-capture rig + release CI + gh-pages landing site (branch `build/excel-backup`):** brought myHealth to parity with myFinance's release tooling. **myHealth is already public at `tokans/myHealth`** (no private→public mirror like myFinance's `anshumandas`→`tokans`), so `build-release.yml` runs **directly** in this repo (job guard `github.repository == 'tokans/myHealth'`, built-in `GITHUB_TOKEN`, no `mirror-to-public.yml`). (1) **Demo rig** (`demo/`, dev-only): a thin app-specific layer over the shared **`@mydemo/core`** package (added as an **optional** dep `file:../myDemo`; `tsx`+`webdriverio` devDeps) — `demo/config.ts` (identity `myHealth`/`com.myhealth.app`/`myhealth`, `navAnchor` = the `today-root` test-id since the shared `SuiteShell` nav has no per-link ids, so scenarios navigate by `h.goto(route)`; `build.frontendEnv` bakes `VITE_DEMO_MODE=1` + `VITE_TIER=champion` + `VITE_ALLOW_TIER_OVERRIDE=1` + `VITE_SEED=on`), `build`/`reset`/`record` thin wrappers, `run-demo.ps1`/`make-video.ps1` launchers, `edit/` (marketing EDL + tutorial), and **8 scenarios** (`01-welcome-profile`, `02-log-vital`, `03-goal-eta`, `04-trends`, `05-schedule-reminder`, `06-medication`, `07-document-scan`, `20-full-tutorial`). Product **demo-mode hooks** (`src/lib/demoMode.ts` — `DEMO_MODE`/`DEMO_MASTER_PASSWORD`/`demoSaveName`, all constant-folding away in normal builds): vault **auto-unlock** in `UnlockPanel.tsx`, native-save **dialog-skip** in `ExcelButtons.tsx`/`Documents.tsx`/`excelBackup.ts`, and the Rust **`--demo` window-maximize** in `src-tauri/src/lib.rs` (`#[cfg(all(desktop, debug_assertions))]`). Scenarios click kebab-case `data-testid`s added only to the elements they touch (`today-*`, `metrics-*`, `profile-*`, `goal-*`, `block-*`, `med-*`, `reminder-*`, `trends-kind`, `documents-*`). `demo/output/`+`demo/.bin/` git-ignored; `DEMO.md` documents it (the document-scan file-pick uses a native OS dialog, so its tail is hand-captured). (2) **Release CI** (`.github/workflows/build-release.yml`): version → build (Windows staging + macOS universal) → sign-windows (SignPath pass-through, `project-slug: myhealth`) → build-android + build-ios (both `continue-on-error`) → publish (GitHub Release + regenerated README + gh-pages). Mobile uses ported support scripts: `scripts/android/Dockerfile.libsodium` + `build-libsodium-android.sh` (myHealth also links `tauri-plugin-stronghold`→libsodium, so the version-exact 1.0.22 cross-build applies unchanged) and `scripts/ensure-ios-plist.mjs` (`ios:plist` npm script; `_myhealth._tcp` Bonjour + LAN-sync/camera usage strings). CI installs sharedCoreLib **and** scandoc-lib deps so `prebuild` can build both. (3) **gh-pages** (`.github/pages/`): teal-rebranded `index.template.html` (placeholders `__VERSION__`/`__REPO__`/`__RELEASE_URL__`/`__LATEST_URL__`; the demo `<video>` self-hides until `assets/demo.mp4` is recorded/committed), `serve.py`/`preview.bat`/`README.md`/`sample-release-notes.md`. `security.yml` unchanged but for a stale-comment fix. **No SQLite migration** (migrations stay **0001–0009**). `npm run build` + `npm test` (**406**) green; demo secrets confirmed absent from a normal `dist/`.

---

## Project summary

A private, offline, **client-only** family health companion. Build profiles + goals for self & family, track metrics/vitals, and import medical documents (prescriptions + lab reports) via a local CPU pipeline.

**Independence:** myHealth is **fully standalone** — it must build and run without any other app installed. It is one of a family of independent local-first apps that *optionally* share a common substrate package (`sharedcorelib`); it does **not** depend on myFinance or any sibling app. See `docs/SHARED_CORE.md`. Read `docs/PRODUCT_FEATURES.md`, `docs/PLAN.md`, and `docs/SHARED_CORE.md` before making architectural decisions.

## Hard constraints (do not violate without owner sign-off)

- **Client-only, no backend, no web target.** All data on-device (SQLite + encrypted vault).
- **Receive-only network.** Pull signed public reference data only; upload nothing. No analytics, no phone-home.
- **No medical interpretation** — extract / record / trend / remind only. No diagnosis, advice, or drug-interaction checking. Disclaimers on any interpretive-looking surface.
- **No cloud LLM; no LLM in product logic.** Everything deterministic. The *only* ML is small, **local, CPU** OCR models in the import pipeline — **never authority**; drug name, dosage, and lab values from non-native-text sources are **confirm-required** (human-in-the-loop).
- **Append-only schema evolution.** Schemas are `SchemaDescriptor`s (additive) + append-only **aux-SQL** steps against `suite.db` (never edit an applied aux step — add a new version). The retired Tauri-plugin migration files (`src-tauri/migrations/0001–0009`) are historical only.
- **Gated master content is double-gated** — visible only when the user's tier is reached **AND** the owner has published a signed bundle. No locked teaser before data exists.
- **Progressive disclosure is mandatory.** Newcomers see a calm, small app (Starter tier). Features are **Open / Nudge (shown-locked with a one-line CTA) / Hidden (not rendered)**, and disclosure reveals **exactly one tier ahead** — a feature is a Nudge only one tier below its unlock and Hidden any further down (`gateVisibility` in `src/lib/featureGate.ts`). Tier ladder: 🌱 Starter → 📈 Tracker → 🧭 Caretaker → 🏆 Champion (+ grant tiers 💗 Supporter, ✅ Verified Pro). The ladder is app-defined config on the shared tier engine; unlock signals are **local-only** telemetry. See `docs/PRODUCT_FEATURES.md` §4.J and §6. Do not surface advanced features to a Starter user.
- **Shared-core install is refcounted, with a standalone fallback.** Heavy runtime assets (OCR sidecar/models, OTA masters cache) install once into a shared per-user suite dir and are reused by sibling apps; the app must still run from its **own bundled copy** if the shared dir is absent. Removing one app must never break another. Never share the vault, DB, or settings. See `docs/SHARED_CORE.md` §7.

## Planned technology (shared local-first stack — see `docs/PLAN.md` §1)

Tauri v2 (Rust shell) + React 18 + TypeScript + Vite · HashRouter · Zustand · TanStack Query · Tailwind + shadcn/ui · SQLite (`tauri-plugin-sql`) over the shared **`suite.db`** (one DB; app tables namespaced `myhealth_*`) · Stronghold vault + per-device DEK + AES-256-GCM doc blobs (and the sealed `extracted_text_enc`) · Ed25519-signed/AES-GCM OTA masters from GitHub Releases · `tauri-plugin-notification` reminders · optional LAN device sync · path alias `@/` → `src/`.

**The one net-new, app-specific subsystem is the document-import pipeline** (`docs/medical-document-parser-architecture.md`), likely a bundled local OCR sidecar — decision pending (see `docs/PLAN.md` §4). Everything else is shared-core (`docs/SHARED_CORE.md`).

## Architecture (scaffold landed — keep in sync as modules land)

> The skeleton below now exists and builds. Sections marked _(later)_ are stubs/placeholders pending their roadmap phase.

- `src-tauri/` — Rust shell: `lib.rs` registers plugins (the SQL plugin stays, but its per-app migration array is RETIRED — tables come from descriptors + aux-SQL on the JS side); `core_bootstrap.rs` lays down/reuses the per-user suite dir and exposes `shared_core_db_path` (the shared `suite.db`); `capabilities/default.json` is the permission allowlist; possible parser-sidecar bridge.
- `src/pages/` — Today (daily tasks + water), Profiles, Goals, Metrics, Schedule, Import, Medications, Vault, Timeline, Immunizations, Directory (gated), etc.
- `src/domain/` — **pure** deterministic logic (goal projection, trend/range flagging, vaccine/screening schedules, care-plan templates, ICE). No DB, no React.
- `src/content/` — myHealth's **bindings of the shared content framework** (`sharedcorelib/content`): `model.ts`/`schema.ts` re-export core types/helpers/schemas pinned to Lucide icons + the app's `EarnedTier`; `registry.ts` runs the app's own `import.meta.glob('/content/*/index.ts')` + `icons.ts` resolver and calls the core merge; `updater.ts` = `createContentSync(...)` with the app's env + store. The per-type folders live at the repo-root **`content/`** (e.g. `content/yoga/`, `content/exercises/`) with baked samples in `index.ts` and downloadable packs in `bundles/` (`tsconfig` includes `content`).
- `src/db/` — typed wrappers over the **shared `suite.db`** (`client.ts` opens it via `shared_core_db_path`; `getDb()` throws outside Tauri — gate with `isTauri()`). Tables are namespaced `myhealth_*` (`tables.ts` = the name constants), defined by descriptors in `appTables.ts` + aux-SQL in `auxMigrations.ts`, registered by `sharedDb.ts`/`schemas.ts`. `consolidate.ts` is the one-time legacy-DB migrator; `sealedText.ts` seals `extracted_text`. The common `common#IceCard` emergency-card store (`sharedcorelib/ice`) and the person spine are read/written by both myHealth and myFinance.
- `src/import/` — **health-specific** document field-extraction (IMPLEMENTED): `extractPrescription` / `extractLab` / `parseDocument` over recognized text, `formulary.ts` (drug brand→generic), `labVocab.ts` (LOINC-style tests), `openmed.ts` (sidecar stub), and `__fixtures__/` of difficult OCR documents. The **domain-agnostic reading engine** that used to live here (`normalize.ts` OCR cleanup, `fuzzy.ts` Levenshtein/ranking, `confidence.ts` tiering + confirm-required) has been extracted to the shared **`@scandoc/core`** package (`sharedCoreLib/scandoc-lib`, added as `file:../sharedCoreLib/scandoc-lib`) so myDocs and any future document-reading app reuse one engine; `src/import/` imports those primitives from `@scandoc/core` and `import/index.ts` re-exports them so the `import/` surface is unchanged for callers. `requiresConfirmation` is now domain-agnostic (`(source, safetyCritical)`); myHealth supplies `safetyCritical: true` for drug/dosage at the call site. The extractors are **pure** — no DB/network/models. The **`Recognizer` seam now has a real, free implementation: Tesseract** (`@scandoc/core/ocr`, see the OCR note above) — image/scanned-PDF bytes are OCR'd on-device (WASM), `src/import/ocr/` is the app glue, and the captured/extracted text feeds the same extractors. The paid **OpenMed** AI extraction (`openmed.ts` stub) stays the separate subscription path. Still pending: the capture-quality-gate and the confidence-tiered review UI / correction logging.
- `src/test/` — Vitest setup (`setup.ts`: jsdom + `@testing-library/jest-dom`). Tests are colocated `*.test.ts(x)` beside the code; run via `npm test`. Config: `vitest.config.ts`.
- `src/vault/` — Stronghold + document crypto (ported from myFinance).
- `src/masters/` — registry + OTA + verify (ported); planned bundle types: providers directory, health-items catalog. (The **content framework** in `src/content/` uses the same shared `sharedcorelib/masters` OTA engine for its folder-driven downloadable tabs — see the content-framework note above.)
- `src/stores/` — settings, profiles, tier, gating (Zustand).
- `src/lib/` — environment, tiers/gamification, gating, notify, report export, `openExternal` (OS browser opener), `reportIssue` (prefilled GitHub issue URL builder).
- `src/components/feedback/` — `ReportIssueDialog` (suite-standard "Report an issue" flow → GitHub).

## Shared-core map (consume from `sharedcorelib`, don't reinvent)

Vault & doc crypto · masters/OTA · tiers & feature-gating · **content library framework** (`sharedcorelib/content` — folder-driven content tabs: model/helpers/schemas, `createContentStore`, `createContentSync`, registry merge; the app supplies the `content/<type>` glob, icons, page, and release/keys) · reminders (derived + manual + OS notifications) · report/PDF export · ICE/emergency (incl. the common `IceCard` table + `createIceStore`) · **shared suite DB + schema registry** (`sharedcorelib/db` + `/schema`) · LAN sync · UI primitives (incl. `SupportedByTokans` attribution). These are app-agnostic mechanisms parameterized by app-supplied config (dependency injection). See `docs/PLAN.md` §5 and `docs/SHARED_CORE.md`. If the shared package isn't available yet, implement in-app and extract later — **never** add a runtime dependency on a sibling app. The shared `suite.db` is **not** a dependency on a sibling app — both apps talk to the shared substrate independently and each still runs standalone (the ICE store no-ops when `suite.db` is absent).

## Commands

`npm run dev` (Vite browser, no DB/vault), `npm run tauri:dev` (full app — required for SQLite/Stronghold), `npm run build` (runs `prebuild` = builds `../sharedCoreLib`, then tsc --noEmit + Vite), `npm run tauri:build`, `npm run test` (Vitest, `*.test.ts` colocated). The frontend currently builds green via `npm run build`.

## Related context

- The shared substrate (and its DI/config contract) is described in `docs/SHARED_CORE.md`. The proven precedent for this extraction style is the `@mydemo/core` package (a demo rig extracted into a standalone `file:../` package with injected config) — the shared core follows the same dependency-injection discipline.
- myHealth is **independent**: do not introduce build/runtime coupling to myFinance. Cross-app interop (if any) is **file-based and optional** (e.g. exporting a summary another app can import), never a code dependency.
