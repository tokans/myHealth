# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## ⚠️ Living documentation contract (READ FIRST, applies every session)

myHealth is an **evolving project**. These three documents are the source of truth and **must be kept current as the project grows**:

- `README.md` — user-facing overview, status, feature summary, layout.
- `CLAUDE.md` — this file: agent guidance, architecture, constraints, conventions.
- `docs/PRODUCT_FEATURES.md` and `docs/PLAN.md` — the product + plan specs.
- `docs/SHARED_CORE.md` — how this app relates to the shared cross-app core package.

**The rule:** whenever you complete a **major feature add or an architectural change**, you MUST, in the *same* piece of work, update the affected docs above before considering the task done. "Major" = a new top-level feature/page/module, a new migration set, a new subsystem (e.g. the import sidecar), a new gate/tier, a new master/OTA bundle type, or any change that makes the existing docs inaccurate.

Concretely, on each major change:
1. Update `CLAUDE.md` — add/adjust the relevant architecture section, the migration count, the data-model sketch, and the "current state" line below.
2. Update `README.md` — feature list, status, and roadmap if the phase advanced.
3. Update `docs/PLAN.md` (roadmap/data-model) and `docs/PRODUCT_FEATURES.md` (feature catalog) if scope shifted.
4. Mention the doc updates in your summary so they're visible in review.

Do **not** treat docs as optional cleanup. A feature is not "done" until the docs describe it. If a change is small/mechanical (a bugfix, a rename, a style tweak), no doc update is needed — use judgement.

**Current state:** _Phase 1 complete._ Tauri+React+TS app wired to `sharedcorelib` (vault, tiers, gating, reminders sweep, ice, report, env, ui, db/schema, common masters). Migrations 0001–0009. Working: **Today** (water + daily tasks), **Profiles** (self+family), **Vitals**, **Reminders** inbox, Tracker-tier **Goals** (deterministic ETA — `domain/goals.ts`) / **Schedule** / **Medications** / **encrypted Document vault** (Stronghold unlock + AES-GCM blobs via `sharedcorelib/vault`), Caretaker-tier **Medical ICE card** + **doctor-visit summary** (`lib/visitReport.ts` → HTML→PDF via `sharedcorelib/report`), **Journey** (tier ladder), the progressive-disclosure shell (Open/Nudge/Hidden + `FeatureGuard`), and the **habit reminder sweep** (water/task/medication → one OS notification per sweep, on idle). The shell now also carries the suite-standard **"Report an issue"** action (prefilled GitHub issue via `lib/reportIssue.ts` + `lib/openExternal.ts`) and the **"Supported by Tokans.org"** attribution line (`sharedcorelib/ui` → `SupportedByTokans`). The **Medical ICE card is now a shared common table** in the suite DB: `common#IceCard` (`sharedcorelib/ice` → `ICE_CARD_SCHEMA` + `createIceStore`), keyed per person, written/read by both myHealth and myFinance through the shared `suite.db` (Rust `shared_core_db_path` + `db/sharedDb.ts`); whichever app launches first creates it. **Newcomer (Starter) surface is water-first:** only **Today** (water + tasks), **Vitals**, **Reminders**, **Profiles**, and **Journey** are Open; **Goals** and **Schedule** are now **Hidden** until Tracker (alongside trends/medications/documents); **Import medical documents** is a **Nudge** (shown-locked even at Starter) so newcomers see the path to it without clutter — see `src/lib/featureGate.ts`. **Active profile is now a global store** (`src/stores/profile.store.ts`, selected id persisted to localStorage) backing `useActiveProfile`, so switching the active person re-scopes every page; in `nav.ts`, `primary` now means "mobile bottom-bar tab." The **app shell is now the shared `SuiteShell`** (`sharedcorelib/ui`): `AppShell.tsx` is a thin adapter that feeds it nav data (with precomputed open/nudge `state`), `centralActions`, `actions`, a tier badge, and the `profile` slot. Layout = desktop sidebar + mobile top bar (brand left, profile right) + a fixed three-button bottom bar **[Today (home) · Vitals (center) · More]** (the center button is adaptive — a plain button at 1 action, a FAB + bottom sheet at 2+); the **More** drawer (built into `SuiteShell`) holds the remaining destinations + "Report an issue" + the Tokans attribution. The top-right `profile` slot is `ProfileMenu.tsx` (avatar button + the family `ProfileDrawer.tsx`); `components/ui/sheet.tsx` is now a re-export shim of the shared `Sheet`, and the old `MoreDrawer.tsx` was deleted (superseded by `SuiteShell`'s built-in drawer). Theming is the shared **§4.2** model: `tailwind.config.cjs` uses `sharedcorelib/tailwind-preset` + adds `../sharedCoreLib/src/ui/**` to its content globs, and `src/index.css` imports `sharedcorelib/ui/theme.css` then overrides only myHealth's ~5 teal brand tokens. The first slice of **Phase 2** has landed: the **document-import field-extraction layer** (`src/import/`) — a pure, deterministic engine (no DB/network/models/LLM) that turns *recognized* prescription/lab text into structured, confidence-tiered, human-confirmable fields. It fuzzy-matches against a small India-context drug **formulary** (`formulary.ts`, brand→generic) and a **LOINC-style lab vocabulary** (`labVocab.ts`), tolerates OCR noise (`normalize.ts`: O/0, l/I/1, S/5 fixups; India-style positional `1-0-1` dosing; en-dash ranges; digits embedded in names like HbA1c), tiers each field `auto`/`disambiguate`/`manual` (`confidence.ts`), and keeps drug/dosage **confirm-required** (`verified:false`). Entry points: `extractPrescription` / `extractLab` / `parseDocument` (`index.ts`). It is decoupled from recognition: the upstream capture/normalization/**OCR sidecar** (parser architecture doc §6.1–6.5; sidecar-vs-native still pending) is the remaining Phase-2 work, so extraction is tested today against deliberately difficult OCR text fixtures (`src/import/__fixtures__/documents.ts`). The whole app is now covered by a **Vitest** suite (jsdom + `@testing-library/react`): **283 tests** spanning domain logic, all `db/` wrappers (mocked `./client`), every store, the React components + `useActiveProfile`, and the import extractors — config in `vitest.config.ts`, setup in `src/test/setup.ts`, tests colocated as `*.test.ts(x)`. `npm run build` + `npm run test` (283 tests) green. Migrations remain **0001–0009** (the extraction layer added no schema). Next: the import **OCR/capture sidecar** + confidence-tiered review UI to feed `src/import` (`docs/PLAN.md` §4), plus OTA masters (Phase 4). Gated placeholders remain for trends/import/directory. Update this line as phases land (see `docs/PLAN.md` §6).

**Stage C migration (branch `build/stageC-migration`, sharedCoreLib v0.2.0):** myHealth now sits on the suite's **shared-entity spine**. Profiles (self + family) and **pets (modeled as managed-dependent `person` rows with `relationship_to_self = 'pet'`)** reference core **`person`** (`sharedcorelib/entities`); the medical data myHealth OWNS lives in a per-app **`myhealth#HealthFacet`** table keyed by `person_key` (sex/blood_group/height/conditions/medications/allergies/advance_directive/organ_donor/is_pet) — field-level ownership, with contact staying on `person`/the common ICE card (finance/ICE precedent). Wiring: `src/db/healthFacet.ts` (facet schema + store), `src/db/entities.ts` (`createHealthPeople`: person spine + facet + legacy-profile migration), registered through `src/db/schemas.ts` (`MYHEALTH_SCHEMAS` now = ICE card + the common `ENTITY_SCHEMAS` + the health facet; the only myHealth-owned table is the facet, the rest are common/non-duplicated) and `src/db/sharedDb.ts` (`healthPeople()`/`healthTimeline()`/`healthBreakGlassContributor()`). myHealth is the suite's **second consumer of core break-glass** (`src/breakglass/contributor.ts`): a `BreakGlassContributor` exposing emergency medical (nominee), advance_directive (executor) and full notes (full) — sealed by core under an out-of-band passphrase; **no health data egresses**. Medical documents + visits/appointments project onto the shared `document`/`event` spine (`src/db/sharedTimeline.ts`) — only the OPAQUE vault `blob_ref` is stored; **document bytes stay AES-GCM under the per-device key**. Layered **recovery** adopted (`src/vault/recovery.ts`: free on-device floor + optional registered-tier ciphertext escrow). Person-linked **reveal ladder + myLifeAssistant nudge** (`src/lib/reveal.ts`). **OpenMed (Phase 4) is a capability-detection STUB only** (`src/import/openmed.ts`) — the Python sidecar isn't built; the free deterministic extraction path is UNCHANGED and the stub always reports the sidecar absent (no network, double-gated on sidecar + entitlement). The app-local `myhealth.db` (migrations 0001–0009) is untouched. +42 Stage-C tests (`*.test.ts` colocated).

**Excel backup/restore (branch `build/excel-backup`, sharedcorelib v0.3.0):** a new **Settings** page (`/settings`, listed in the More drawer) hosts the suite-standard whole-store backup — `sharedcorelib/backup` subsystem #22 via the shared `BackupPanel` (`sharedcorelib/ui`). It exports ALL of `myhealth.db` + myHealth's slice of the shared `suite.db` (facet + common spine, from the registry) to one `.xlsx` (a sheet per table) and re-imports it on another machine; Secret-tier/password-named fields export as one-way sha256 fingerprints and are skipped on import; vault blobs are never exported. Wiring: `src/lib/excelBackup.ts` (+ `openSharedDbAdapter` in `db/sharedDb.ts`), `src/pages/Settings.tsx`.

**Shared scanned-document reading engine (branch `build/scandoc-lib`):** the domain-agnostic reading primitives that lived in `src/import/` — `normalize.ts` (OCR-noise cleanup), `fuzzy.ts` (Levenshtein/`rankMatches`), `confidence.ts` (tier + confirm-required) — were extracted into a new standalone package **`@scandoc/core`** (`sharedCoreLib/scandoc-lib`, added the same way as `sharedcorelib`/`@form-engine/react`: `file:../sharedCoreLib/scandoc-lib`), so **myDocs** and any future document-reading app reuse one engine instead of forking it. The package also adds a typed **`Recognizer` seam** — the plug point the future OCR/capture sidecar will implement (no OCR engine ships yet; `nativeTextRecognizer` is the offline identity recognizer) — and an optional `Field<T>`/`fieldFrom` extraction-framework contract. myHealth keeps ONLY its health-specific extractors/vocabulary in `src/import/` (`extractPrescription`/`extractLab`/`parseDocument`/`formulary`/`labVocab`/`openmed`), importing the engine from `@scandoc/core`; `import/index.ts` re-exports the moved symbols so the `import/` surface is unchanged for pages/tests. `requiresConfirmation` is now `(source, safetyCritical)` with myHealth passing `true` for drug/dosage. No behavior change, no new runtime deps, no egress; the 3 moved unit-test files now live in the lib (myHealth `npm test` = 301, was 325). `prebuild` now also builds `scandoc-lib`.

---

## Project summary

A private, offline, **client-only** family health companion. Build profiles + goals for self & family, track metrics/vitals, and import medical documents (prescriptions + lab reports) via a local CPU pipeline.

**Independence:** myHealth is **fully standalone** — it must build and run without any other app installed. It is one of a family of independent local-first apps that *optionally* share a common substrate package (`sharedcorelib`); it does **not** depend on myFinance or any sibling app. See `docs/SHARED_CORE.md`. Read `docs/PRODUCT_FEATURES.md`, `docs/PLAN.md`, and `docs/SHARED_CORE.md` before making architectural decisions.

## Hard constraints (do not violate without owner sign-off)

- **Client-only, no backend, no web target.** All data on-device (SQLite + encrypted vault).
- **Receive-only network.** Pull signed public reference data only; upload nothing. No analytics, no phone-home.
- **No medical interpretation** — extract / record / trend / remind only. No diagnosis, advice, or drug-interaction checking. Disclaimers on any interpretive-looking surface.
- **No cloud LLM; no LLM in product logic.** Everything deterministic. The *only* ML is small, **local, CPU** OCR models in the import pipeline — **never authority**; drug name, dosage, and lab values from non-native-text sources are **confirm-required** (human-in-the-loop).
- **Append-only migrations.** Never edit a shipped migration.
- **Gated master content is double-gated** — visible only when the user's tier is reached **AND** the owner has published a signed bundle. No locked teaser before data exists.
- **Progressive disclosure is mandatory.** Newcomers see a calm, small app (Starter tier). Features are **Open / Nudge (shown-locked with a one-line CTA) / Hidden (not rendered until a tier)**. Tier ladder: 🌱 Starter → 📈 Tracker → 🧭 Caretaker → 🏆 Champion (+ grant tiers 💗 Supporter, ✅ Verified Pro). The ladder is app-defined config on the shared tier engine; unlock signals are **local-only** telemetry. See `docs/PRODUCT_FEATURES.md` §4.J and §6. Do not surface advanced features to a Starter user.
- **Shared-core install is refcounted, with a standalone fallback.** Heavy runtime assets (OCR sidecar/models, OTA masters cache) install once into a shared per-user suite dir and are reused by sibling apps; the app must still run from its **own bundled copy** if the shared dir is absent. Removing one app must never break another. Never share the vault, DB, or settings. See `docs/SHARED_CORE.md` §7.

## Planned technology (shared local-first stack — see `docs/PLAN.md` §1)

Tauri v2 (Rust shell) + React 18 + TypeScript + Vite · HashRouter · Zustand · TanStack Query · Tailwind + shadcn/ui · SQLite (`tauri-plugin-sql`, `sqlite:myhealth.db`) · Stronghold vault + per-device DEK + AES-256-GCM doc blobs · Ed25519-signed/AES-GCM OTA masters from GitHub Releases · `tauri-plugin-notification` reminders · optional LAN device sync · path alias `@/` → `src/`.

**The one net-new, app-specific subsystem is the document-import pipeline** (`docs/medical-document-parser-architecture.md`), likely a bundled local OCR sidecar — decision pending (see `docs/PLAN.md` §4). Everything else is shared-core (`docs/SHARED_CORE.md`).

## Architecture (scaffold landed — keep in sync as modules land)

> The skeleton below now exists and builds. Sections marked _(later)_ are stubs/placeholders pending their roadmap phase.

- `src-tauri/` — Rust shell: `lib.rs` registers plugins + embeds numbered migrations; `core_bootstrap.rs` lays down/reuses the per-user suite dir and exposes `shared_core_db_path` (the shared `suite.db`); `capabilities/default.json` is the permission allowlist; possible parser-sidecar bridge.
- `src/pages/` — Today (daily tasks + water), Profiles, Goals, Metrics, Schedule, Import, Medications, Vault, Timeline, Immunizations, Directory (gated), etc.
- `src/domain/` — **pure** deterministic logic (goal projection, trend/range flagging, vaccine/screening schedules, care-plan templates, ICE). No DB, no React.
- `src/db/` — typed wrappers over migration SQL (`client.ts` lazily opens the app's own `myhealth.db`; `getDb()` throws outside Tauri — gate with `isTauri()`). `sharedDb.ts` + `schemas.ts` wire the **shared suite DB** (`sharedcorelib/db` over `suite.db`), registering myHealth's schemas and exposing the common `common#IceCard` emergency-card store (`sharedcorelib/ice`) that's read/written by both myHealth and myFinance.
- `src/import/` — **health-specific** document field-extraction (IMPLEMENTED): `extractPrescription` / `extractLab` / `parseDocument` over recognized text, `formulary.ts` (drug brand→generic), `labVocab.ts` (LOINC-style tests), `openmed.ts` (sidecar stub), and `__fixtures__/` of difficult OCR documents. The **domain-agnostic reading engine** that used to live here (`normalize.ts` OCR cleanup, `fuzzy.ts` Levenshtein/ranking, `confidence.ts` tiering + confirm-required) has been extracted to the shared **`@scandoc/core`** package (`sharedCoreLib/scandoc-lib`, added as `file:../sharedCoreLib/scandoc-lib`) so myDocs and any future document-reading app reuse one engine; `src/import/` imports those primitives from `@scandoc/core` and `import/index.ts` re-exports them so the `import/` surface is unchanged for callers. `requiresConfirmation` is now domain-agnostic (`(source, safetyCritical)`); myHealth supplies `safetyCritical: true` for drug/dosage at the call site. **Pure** — no DB/network/models. The capture-quality-gate and **OCR sidecar** are still pending (Phase 2) — they will implement the `@scandoc/core` `Recognizer` seam; the confidence-tiered review UI / correction logging are also still pending.
- `src/test/` — Vitest setup (`setup.ts`: jsdom + `@testing-library/jest-dom`). Tests are colocated `*.test.ts(x)` beside the code; run via `npm test`. Config: `vitest.config.ts`.
- `src/vault/` — Stronghold + document crypto (ported from myFinance).
- `src/masters/` — registry + OTA + verify (ported); new bundle types: providers directory, health-items catalog.
- `src/stores/` — settings, profiles, tier, gating (Zustand).
- `src/lib/` — environment, tiers/gamification, gating, notify, report export, `openExternal` (OS browser opener), `reportIssue` (prefilled GitHub issue URL builder).
- `src/components/feedback/` — `ReportIssueDialog` (suite-standard "Report an issue" flow → GitHub).

## Shared-core map (consume from `sharedcorelib`, don't reinvent)

Vault & doc crypto · masters/OTA · tiers & feature-gating · reminders (derived + manual + OS notifications) · report/PDF export · ICE/emergency (incl. the common `IceCard` table + `createIceStore`) · **shared suite DB + schema registry** (`sharedcorelib/db` + `/schema`) · LAN sync · UI primitives (incl. `SupportedByTokans` attribution). These are app-agnostic mechanisms parameterized by app-supplied config (dependency injection). See `docs/PLAN.md` §5 and `docs/SHARED_CORE.md`. If the shared package isn't available yet, implement in-app and extract later — **never** add a runtime dependency on a sibling app. The shared `suite.db` is **not** a dependency on a sibling app — both apps talk to the shared substrate independently and each still runs standalone (the ICE store no-ops when `suite.db` is absent).

## Commands

`npm run dev` (Vite browser, no DB/vault), `npm run tauri:dev` (full app — required for SQLite/Stronghold), `npm run build` (runs `prebuild` = builds `../sharedCoreLib`, then tsc --noEmit + Vite), `npm run tauri:build`, `npm run test` (Vitest, `*.test.ts` colocated). The frontend currently builds green via `npm run build`.

## Related context

- The shared substrate (and its DI/config contract) is described in `docs/SHARED_CORE.md`. The proven precedent for this extraction style is the `@mydemo/core` package (a demo rig extracted into a standalone `file:../` package with injected config) — the shared core follows the same dependency-injection discipline.
- myHealth is **independent**: do not introduce build/runtime coupling to myFinance. Cross-app interop (if any) is **file-based and optional** (e.g. exporting a summary another app can import), never a code dependency.
