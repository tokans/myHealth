/**
 * Scenario contract. Re-exported from @mydemo/core so scenario files keep their
 * local `./types.ts` import; the canonical definitions live in the package.
 */
export type { Helpers, Scenario, Mark } from "@mydemo/core";
